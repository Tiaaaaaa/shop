class Book {
  Book(this.isbn, this.subject, this.title, this.volume, this.publisher,
      this.price, this.section);

  int isbn;
  String subject;
  String title;
  String volume;
  String publisher;
  int price;
  String section;
}
